# name: Update WakaTime Dev Metrics

# on:
#   schedule:
#     # Runs daily at midnight UTC — you can adjust the cron
#     - cron: '0 0 * * *'
#   workflow_dispatch:

# jobs:
#   update-readme:
#     name: Update README with WakaTime Metrics
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Generate WakaTime stats
#         uses: anmol098/waka-readme-stats@master
#         with:
#           WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
#           GH_TOKEN: ${{ secrets.GH_TOKEN }}
#           # Optional flags — uncomment / tweak if you want:
#           # SECTION_NAME: "waka"
#           # SHOW_TOTAL_CODE_TIME: "true"
#           # SHOW_LINES_OF_CODE: "true"
#           # SHOW_UPDATED_DATE: "true"
#           # UPDATED_DATE_FORMAT: "%d/%m/%Y %H:%M:%S"
#           # COMMIT_MESSAGE: "chore: update dev metrics"
#           # COMMIT_USERNAME: "github-actions[bot]"
#           # COMMIT_EMAIL: "41898282+github-actions[bot]@users.noreply.github.com"

#       - name: Commit and push changes
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
#           git add README.md
#           git commit -m "chore: update WakaTime dev metrics" || echo "No changes to commit"
#           git push
